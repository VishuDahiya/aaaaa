import {Stylesheet} from 'react-native';
import colors from '../../styles/colors';

export default Stylesheet.create({
  qr: {
    alignItems: 'center',
    marginVertical: 150,
  },
  text: {
    marginBottom: 30,
  },
});
